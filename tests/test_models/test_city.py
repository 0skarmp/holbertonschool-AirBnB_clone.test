#!/usr/bin/python3
import unittest
from models.city import City


class TestCity(unittest.TestCase):
    def testCity(self):
      obj_city = City.name
      obj_city = State_id.name
      self.assertIsNotNone(obj_city, "State_id is none"
      self.assertIsNotNone(obj_city, "name is None")
